<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:mvc="http://www.springframework.org/schema/mvc"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="
			http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.2.xsd
			http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.2.xsd
			http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.2.xsd
			">
	<!-- 扫描带Controller注解的类 -->
	<context:component-scan base-package="com.exercise.controller"/>
	
	<!-- 加载注解驱动 -->
	<mvc:annotation-driven/>
	
	<!-- 配置静态资源由默认servlet处理 -->
	<mvc:default-servlet-handler/>
			
	<mvc:interceptors>
		<!-- 配置后台登录验证拦截器 -->
		<mvc:interceptor>
			<mvc:mapping path="/admin/**"/>
			<bean class="com.exercise.interceptor.AdminLoginInterceptor"/>
		</mvc:interceptor>
		<!-- 配置前台登录验证拦截器 -->
		<mvc:interceptor>
			<mvc:mapping path="/user/**"/>
			<bean class="com.exercise.interceptor.UserLoginInterceptor"/>
		</mvc:interceptor>
		<!-- 配置用户自动登录拦截器 -->
		<mvc:interceptor>
			<mvc:mapping path="/"/>
			<bean class="com.exercise.interceptor.UserAutoLoginInterceptor"/>
		</mvc:interceptor>
	</mvc:interceptors>
	
	<!-- 视图解析器 -->
	<bean id="viewResolver" class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<!-- 前缀 -->
		<property name="prefix">
			<value>/WEB-INF</value>
		</property>
		<!-- 后缀 -->
		<property name="suffix">
			<value>.jsp</value>
		</property>
	</bean>
	
	<!-- 配置multipart解析器，这个id名字固定，是由核心控制器直接调用的 -->
	<bean id="multipartResolver"
		class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
		<property name="maxInMemorySize" value="1048576"/>
	</bean>
	
</beans>
